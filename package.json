{
  "name": "romantically-fight",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "postinstall": "npm run prisma:generate --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
